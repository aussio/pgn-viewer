import React from 'react';

/**
 * Breadcrumbs
 *
 * Placeholder breadcrumbs for branch hierarchy navigation.
 * Replace with dynamic content as needed.
 */
const Breadcrumbs: React.FC = () => (
  <div>
    <span>Home</span> &gt; <span>Main Line</span> &gt; <span>Current Variation</span>
  </div>
);

export default Breadcrumbs; 