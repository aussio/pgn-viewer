import React from 'react'
import type { FC } from 'react'
import HomePage from './pages/HomePage'

/**
 * App
 *
 * Root component for the PGN Viewer app. Renders the HomePage.
 * @returns {JSX.Element} The rendered app.
 */
const App: FC = () => {
  return <HomePage />
}

export default App
